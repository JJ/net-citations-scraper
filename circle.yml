dependencies:
  pre:
    - curl -L https://cpanmin.us | perl - --sudo App::cpanminus
  post:
    - cpanm --local-lib=~/perl5 --installdeps .
test:
  override:
    - export PERL5LIB=$PERL5LIB:~/perl5; perl Makefile.PL INC=-I~/perl5  && make && prove -l ~/perl5