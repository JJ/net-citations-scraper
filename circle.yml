dependencies:
  pre:
    - curl -L https://cpanmin.us | perl - --sudo App::cpanminus
  post:
    - cpanm --local-lib=~/perl5 --installdeps .
test:
  override:
    - export PERL5LIB=$PERL5LIB:~/perl5; perl Makefile.PL PREFIX=~/lib LIB=~/lib  && make && prove -l ~/perl5